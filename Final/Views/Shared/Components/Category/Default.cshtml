@model List<Category>

<div class="col-md-12">
    <div class="row">
        @foreach (Category item in Model)
        {
            <a href="/product/index/@item.Id">@item.Name</a>
        }
    </div>
</div>