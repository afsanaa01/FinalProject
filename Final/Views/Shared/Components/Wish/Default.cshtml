@model List<Wish>
@if (Model.Count == 0)
{
    <i class="bi bi-heart" style="cursor: pointer;"></i>
}
else
{
    <i class="bi bi-heart" style="cursor: pointer;"></i>
    <div class="numberCircle">@Model.Count()</div>
}